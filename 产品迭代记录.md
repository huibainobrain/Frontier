# FrontierLit 产品迭代记录

> 这份文档记录的不是代码改动，而是开发过程中出现的 **产品问题 → 产品判断 → AI Coding 落地 → 验证结果**。纯工程重构和无明显用户价值的技术细节不在此展开。

## 1. 从固定流程到动态重规划

**问题**  
复杂比较 / 追踪任务中，一次检索经常只能覆盖部分研究对象或维度。固定 Workflow 即使继续搜索，也无法针对“当前具体缺什么”调整动作。

**方案**  
将主链路改成：

```text
规划 → 检索 → 分析 → 证据充分度判断
→ [不足] 识别证据缺口 → 改写 Query / 重选数据源 → 定向补搜
```

Evidence Evaluator 同时负责“是否继续”和“下一步做什么”，避免为了形式拆成多个 Agent。

**验证 / 效果**  
真实 Comparison 任务中运行到第二轮时，系统会根据第一轮证据状态改写 Query、收窄 Source，而不是机械重复第一轮搜索；同时受最大轮次和全局文章预算控制。

---

## 2. 修复“Planner 规划了，但 Scout 没照着执行”

**问题**  
早期 Scout 会把 Planner 选择的数据源与默认 Source 合并，导致 Planner 即使做出了明确选择，执行层仍可能扩大搜索范围，出现“规划和执行脱节”。

**方案**  
让首轮搜索严格执行 Planner 的 Source Plan；只有 Planner 无可用 Source 时才走降级默认。官方源 / RSS 的候选也根据当前 Query 做相关性筛选，而不是只按最新内容取样。

**验证 / 效果**  
测试和真实运行中，Round 1 / Round 2 的实际数据源能够与 Planner / Replanner 决策对应；不同 Query 也会改变候选内容选择。

---

## 3. 区分“研究对象”和“检索来源”

**问题**  
Comparison 场景里，OpenAI / Anthropic 是用户要比较的对象，而 arXiv / Semantic Scholar / OpenAlex 只是找资料的来源。两者混用会让最终答案的比较对象发生偏移。

**方案**  
拆分三个概念：

- `research_targets`：用户真正研究 / 比较的对象；
- `retrieval_sources`：当前轮使用的数据源；
- `used_sources`：实际执行过的来源记录。

如果 Comparison 的研究对象缺失，再单独从原始用户问题恢复，而不从检索结果反推。

**验证 / 效果**  
Comparison 可以保持 OpenAI / Anthropic 为固定研究对象，同时动态切换 arXiv、官方源等检索渠道，避免“Source 变成 Target”。

---

## 4. 让同一个问题适配不同用户背景

**问题**  
同一个研究问题，对不同角色、专业方向和研究经验的人，并不存在唯一“最好”的答案。初入领域的用户更需要概念脉络和基础材料；有较强研究经验的用户更关注技术差异、原始证据和前沿进展；已经读过部分材料的用户也不应该被重复推荐相同内容。

**方案**  
在主链路中引入 `UserProfile`，目前实际包含：

- `interests`：关注的研究方向；
- `role_target`：用户角色 / 使用目标；
- `seniority`：经验与研究阶段；
- `reading_history`：已阅读内容。

画像信息会贯穿 Intent、Planner 和 Synthesizer：

- **任务理解**：当 Query 存在多种合理解释时，结合角色和兴趣辅助判断更合适的任务模式；
- **研究规划**：Planner 在选择数据源时同时考虑用户目标和兴趣，而不是只看 Query 字面；
- **输出生成**：所有生成任务都会带入角色、经验层级和兴趣信息，为答案重点与表达深度提供个性化上下文；
- **阅读路径**：基于兴趣生成学习目标，自动跳过已读内容，并按“基础 → 进阶 → 前沿”组织材料和推荐顺序。

**验证 / 效果**  
底层已经打通“用户画像 → 任务理解 / 研究规划 / 最终生成”的个性化链路，Reading Plan 还会显式使用阅读历史避免重复推荐。当Web端把画像采集完整产品化，允许用户在个性化设置模块中自行设置，并细化为教育背景、领域熟悉度、研究能力等独立维度。

---

## 5. 从“临时 RAG”改成真正可积累的研究记忆

**问题**  
如果 RAG 只负责当前任务检索，系统每次都从头研究，过去已经分析过的内容无法形成长期积累。

**方案**  
建立：

```text
Analyze → SQLite 持久化
        → Chroma 向量索引
        → 后续任务按需召回
```

同时保留标题、机构、时间、URL、关键结论、证据片段、置信度等来源信息。

**验证 / 效果**  
历史分析结果可以在后续研究中被再次召回；但产品规则明确：历史 Memory 只做背景，最终事实仍必须由当前证据支撑。

---

## 6. 修复“有引用 ≠ 引用真的支持结论”

**问题**  
早期只检查 Citation 是否存在，无法识别“引用存在，但 Evidence 并不支持 Claim”的情况；如果让 Synthesizer 自己生成 supporting quote，又会形成自证循环。

**方案**  
将事实校验改成：

```text
真实 cited post
→ Evidence
→ Claim
```

规则负责 Citation / Post ID 等结构校验，模型负责 Supported / Partial / Unsupported / Contradicted 语义判断。发现无依据结论后触发修订，并再次校验。

**验证 / 效果**  
真实闭环测试中，未被证据支持的 Claim 会先被识别，再进入 Revision，修订后的答案重新经过同一 Critic；Output Guard 不再自行调用 LLM 补写事实。

---

## 7. 区分“事实不成立”和“模型服务暂时不可用”

**问题**  
如果事实校验 API 超时 / 503，却把结果直接标成 `unsupported`，用户会被误导为“证据不支持”，实际只是校验没有成功执行。

**方案**  
增加 `verification_unavailable` 状态，并统一 Retry；不同模块在 Retry 耗尽后的降级语义单独设计，而不是所有失败都使用同一种 fallback。

**验证 / 效果**  
服务异常不再被包装成事实判断结果；同时局部分析失败可隔离，避免单篇文章失败直接拖垮整次研究。

---

## 8. 避免模型故障把明显的 Comparison 误降级成 Digest

**问题**  
Intent 模型调用失败时，如果统一 fallback 到 Digest，会把明显的比较 / 追踪 / 阅读计划任务路由到错误产品模式。

**方案**  
为高置信的显式表达增加轻量确定性恢复：

- `compare X and Y` / `X vs Y` → Comparison；
- 明确追踪表达 → Tracker；
- 明确学习计划 → Reading Plan；
- 明确摘要 → Digest；
- 仍然不确定 → 标记 unavailable，而不是强猜。

**验证 / 效果**  
基础 Intent 不再因为瞬时 LLM 故障直接改变任务形态；同时避免再引入一套复杂分类器。

---

## 9. 避免 Comparison 缺少研究对象时“从文章来源里猜”

**问题**  
如果 Planner 没有正确返回 Comparison Target，直接用检索到的机构 / Source 猜研究对象，可能把检索来源误当成用户真正想比较的对象。

**方案**  
仅使用原始 `user_goal` 做一次轻量 Target Recovery；恢复失败则诚实说明无法确认，不从 Post / Source 反推。

**验证 / 效果**  
研究对象恢复与检索内容解耦，减少“系统搜到了谁，就拿谁来比较”的逻辑错误。

---

## 10. 零有效证据时禁止继续“生成一个看起来完整的答案”

**问题**  
如果 Scout 有结果，但所有文章分析都失败，继续进入 Synthesizer 会形成无证据生成风险。

**方案**  
增加 Zero-Evidence Guard：

- 首轮无有效分析结果 → 直接停止；
- 后续轮次全部失败，但之前已有有效证据 → 保留之前证据；
- 最终进入 Synthesis 前再次检查有效证据数量。

**验证 / 效果**  
将“没有搜到”“搜到了但分析失败”“已有部分证据”分成不同产品状态，避免空证据下强行输出。

---

## 11. 控制 Agent 自主探索的成本与重复劳动

**问题**  
多轮 Agent 很容易出现重复搜索、无限继续、文章数量失控，质量收益未必覆盖成本。

**方案**

- 最大研究轮次；
- 全局文章预算；
- Canonical URL + 跨轮去重；
- Agent 软停止 + Workflow 硬停止；
- 记录 Stop Reason / Research Rounds / Token / Latency。

**验证 / 效果**  
Agent 可以自主决定下一步，但无法无限扩张；重复 URL 不再持续消耗分析预算，也为后续判断“Replan 到底值不值”提供成本数据。

---

## 12. 把“最终答案好不好”拆成可定位的产品问题

**问题**  
如果只看最终答案，很难区分是 Planner 漏项、检索召回不足、Evaluator 判断错误、Synthesizer 漏写，还是 Critic 误杀。

**方案**  
建立三层评估：

- **结果质量**：任务成功、需求覆盖、引用支撑；
- **Agent 过程**：证据覆盖、证据缺口识别、重规划增益；
- **效率成本**：调用次数、Token、时延。

同时增加 Workflow / Agent 消融开关、不同 Case 运行隔离与过程 Trace。

**验证 / 效果**  
可以沿真实 Bad Case 向下定位模块，并比较 Agent 的质量增益和成本增量，而不是只看一个总分。

---

## 13. 成本受限时，不让产品架构绑定免费模型

**问题**  
作品集 / 产品验证阶段使用高阶 API 全量跑复杂研究成本较高，但如果产品链路写死在低成本模型上，又会把“当前开发配置”变成系统能力上限。

**方案**  
模型调用按节点分层并与产品流程解耦：

- 当前优先使用免费额度 / 免费层 API；
- Model / API Provider 可通过配置替换；
- Planner / Evaluator / Critic 等关键判断节点可以独立升级；
- 后续可采用“简单节点低成本模型、关键节点高阶模型”的分级策略。

**效果**  
当前可以低成本验证产品逻辑和 Agent 架构；未来升级模型时无需重做产品主链路，也可以单独评估每个关键节点的模型 ROI。

---

## 迭代方法总结

这套迭代并不是按技术栈逐项补功能，而是遵循：

```text
真实 Case / Bad Case
→ 判断问题发生在哪个产品环节
→ 设计最小必要改动
→ AI Coding 落地
→ 测试 / Live Run 验证
→ 纳入回归与评估体系
```

因此 AI Coding 在项目中的角色是 **加速实现与验证**；产品问题定义、方案取舍、验收标准和后续优先级仍由产品判断驱动。
