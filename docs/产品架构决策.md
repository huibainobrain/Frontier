# FrontierLit 架构决策

本文只记录几个真正影响产品效果、成本和可控性的架构选择。

## Decision 1：为什么从单轮流程升级为 Agentic Workflow

**触发问题**

复杂比较 / 追踪任务中，第一轮经常出现“某些对象或维度证据充分，另一些明显不足”。固定流程无法根据中间结果决定：要不要继续搜、缺什么、下一轮搜什么。

**选择：Plan → Evaluate → Replan**

- 研究任务适合先拆目标、时间和来源约束；
- 是否补搜取决于第一轮真实证据状态；
- Query 和数据源可以针对具体缺口调整；
- 外层 Workflow 继续控制轮次和成本；
- 可直接评估“重规划增益”和“无效重规划率”。

**代价**：增加模型调用、Token 和响应时延。

**验证**：比较单轮流程与 Agentic Workflow 的任务成功、证据覆盖、Token 和时延。

---

## Decision 2：为什么不做全 Multi-Agent

判断原则：

> 只有需要模型基于运行时状态做开放判断的节点，才交给 Agent / LLM。

**Agent / LLM**：研究规划、证据缺口判断、是否继续研究、Query 改写、数据源重选、事实语义校验。

**确定性程序**：API / RSS 请求、存储、去重、预算、Retry、Schema 校验。

最终采用 **中央 Orchestrator + 局部 Agent 决策节点**。当前任务主要是串行研究，全 Multi-Agent 会增加状态同步、调用成本和故障面，但没有明确产品收益。

---

## Decision 3：为什么长期记忆采用持久化 RAG

历史研究持续积累：

- 全部塞入上下文 → Token 成本和噪声持续上升；
- 完全不保存 → 每次重复研究。

最终采用：

> **SQLite 保存结构化分析结果 + Chroma 建立向量索引，需要时按需召回。**

边界：

> **历史 Memory 是候选背景，不是事实真相；最终事实仍由当前证据支撑。**

---

## Decision 4：为什么事实校验采用“规则 + 语义校验”

只靠 Prompt 无法稳定保证引用有效；纯规则又无法判断“这段证据是否真的支持这个结论”。

```text
确定性校验
- citation 是否存在
- post_id 是否有效
- Evidence 是否为空
        ↓
语义事实校验
- Supported
- Partial
- Unsupported
- Contradicted
```

权威 Evidence 只来自真实 cited post。发现无依据结论后触发修订并重新校验。

---

## Decision 5：Agent 自主性如何控制

**Agent 负责软判断**：证据是否足够、继续搜索是否还有价值、下一轮搜什么。

**Workflow 负责硬边界**：最大研究轮次、全局文章预算、跨轮去重、Retry 上限、零有效证据硬停止。

> **Agent 决定下一步做什么；Workflow 决定最多允许做到什么程度。**

---

## Decision 6：为什么当前使用免费模型，但不绑定免费模型

当前阶段主要验证产品链路和 Agent 机制，受成本限制优先使用免费额度 / 免费层 LLM API。

设计上将模型调用与产品流程解耦：

- 不同节点可独立配置模型；
- API / Model 可直接替换；
- Agent 主链路无需因模型升级重写；
- 后续可对 Planner / Evaluator / Critic 等关键判断节点使用更高阶模型，对低风险节点保留低成本模型。

因此当前模型选择是 **成本约束下的开发配置**，而不是最终产品对模型能力的判断。

---

## 如何验证这些决策

| 评估对象 | 主要指标 |
|---|---|
| 动态重规划 | 证据覆盖率、重规划增益、无效重规划率 |
| 事实校验 | 无依据结论率、误杀率、修订成功率 |
| 长期记忆 | 历史信息利用、重复搜索减少、Token / 时延 |
| 模型分级 | 质量增益、调用成本、时延 |
| 整体 Agentic Workflow | 任务成功率、需求覆盖率、Token、响应时延 |

最终不是证明“Agent 一定比 Workflow 好”或“更强模型一定全量更好”，而是判断：

> **在哪类任务、哪个节点上，额外智能与额外成本是值得的。**
